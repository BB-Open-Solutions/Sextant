# Audited vulnerability exceptions (scripts/vulncheck.sh). CI fails on any
# CALLED vulnerability that is not listed here; every entry MUST carry a
# reason and a revisit condition, and should be removed the moment the
# toolchain or dependency moves.
#
# Format: one GO-XXXX-NNNN id at the start of a line; the rest is the reason.

# Empty on purpose since 2026-08-18. The one entry that stood here,
# GO-2026-5856 (crypto/tls ECH client privacy leak), is fixed in go1.26.5 and
# the toolchain is now 1.26.6, so the exception did what it promised and left.
#
# An empty list is the state to defend. Seven advisories landed between 13 and
# 18 August and the answer was to move the toolchain, not to write seven
# reasons here. Adding an entry means accepting a reachable vulnerability, so
# it needs a reason somebody else can check and a condition that removes it.
